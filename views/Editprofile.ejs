<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Add your head content here -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Profile</title>
    <link rel="stylesheet" href="/CSS/Editprofile.css" />
  </head>
  <body>
    <div class="container">
      <h1>Edit Profile</h1>
      <form action="/user/editProfile" method="POST">

        <label for="username">Username</label>
        <input type="text" id="username" name="username" value="<%= user.username %>" required />
        
        <label for="email">Email</label>
        <input type="email" id="email" name="email" value="<%= user.email %>" required />
        
        <label for="address">Address</label>
        <input type="text" id="address" name="address" value="<%= userfindresult.length > 0 && userfindresult[0].userProfile.length > 0 ? userfindresult[0].userProfile[0].address : '' %>" />
        
       
        <label for="Phone">Phone</label>
        <input type="number" id="Phone" name="phone" value="<%= userfindresult.length > 0 && userfindresult[0].userProfile.length > 0 ? userfindresult[0].userProfile[0].phone : '' %>" />
        
        
        <label for="state">State</label>
        <input type="text" id="state" name="state" value="<%= userfindresult.length > 0 && userfindresult[0].userProfile.length > 0 ? userfindresult[0].userProfile[0].state : '' %>" />

        <button type="submit">Update Profile</button>
        
      </form>
      <a href="/user/profile">Back to Profile</a>
    </div>
  </body>
</html>